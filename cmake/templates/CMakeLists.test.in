# Automatically generated file; DO NOT EDIT

project( @CM_CURRENT_EXE_LC@ )


set( @CM_CURRENT_EXE_SRC@
@CM_CURRENT_SRC_CPP@
)

set( @CM_CURRENT_LIB_INC@
@CM_CURRENT_SRC_HPP@
)


if( EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/config.in.hpp )
   configure_file( "${CMAKE_CURRENT_SOURCE_DIR}/config.in.hpp" "${CMAKE_CURRENT_SOURCE_DIR}/config.hpp" )
endif( EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/config.in.hpp )

if( EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/custom.cmake )
   include( "${CMAKE_CURRENT_SOURCE_DIR}/custom.cmake" )
endif( EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/custom.cmake )

add_executable( @CM_CURRENT_EXE_LC@ ${@CM_CURRENT_EXE_SRC@} ${@CM_CURRENT_LIB_INC@} )
target_link_libraries( @CM_CURRENT_EXE_LC@ ${ENGINE_LINK} @CM_CURRENT_EXE_DEP@ )

set_target_properties(
 @CM_CURRENT_EXE_LC@
 PROPERTIES
  INSTALL_RPATH ${CMAKE_INSTALL_PREFIX}/lib/engine
  INSTALL_RPATH_USE_LINK_PATH TRUE
)

install( TARGETS @CM_CURRENT_EXE_LC@ RUNTIME DESTINATION bin )

if( EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/data AND IS_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/data )
   install( DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/data DESTINATION share/engineTests/@CM_CURRENT_EXE_LC@ )
endif( EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/data AND IS_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/data )

